name: Release Service

on:
  push:
    tags: ["service-v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing service-vX.Y.Z tag
        required: true
        type: string

permissions:
  contents: write
  packages: write

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.release.outputs.tag }}
      version: ${{ steps.release.outputs.version }}
      image_tag: ${{ steps.release.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
      - id: release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          git fetch origin main
          version="${TAG#service-v}"
          manifest="$(jq -r .version apps/server/package.json)"
          bash tooling/release/validate-tag.sh "${TAG}" service- "${manifest}"
          bash tooling/release/require-legacy-bridge.sh service "${manifest}"
          {
            echo "tag=${TAG}"
            echo "version=${version}"
            echo "image_tag=v${version}"
          } >> "${GITHUB_OUTPUT}"

  dashboard:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate.outputs.tag }}
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.5"
      - run: bun install --frozen-lockfile
      - run: bun run --cwd apps/server build:dashboard-assets
      - uses: actions/upload-artifact@v4
        with:
          name: dashboard-assets
          path: apps/server/dist/dashboard
          if-no-files-found: error

  binaries:
    runs-on: ubuntu-latest
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: bun-linux-x64
            os: linux
            arch: amd64
          - target: bun-linux-arm64
            os: linux
            arch: arm64
          - target: bun-darwin-x64
            os: darwin
            arch: amd64
          - target: bun-darwin-arm64
            os: darwin
            arch: arm64
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate.outputs.tag }}
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.5"
      - run: bun install --frozen-lockfile
      - run: |
          mkdir -p dist
          bun build ./pulse.ts --compile --target=${{ matrix.target }} \
            --outfile ./dist/pulse-server-${{ matrix.os }}-${{ matrix.arch }}
        working-directory: apps/server
      - uses: actions/upload-artifact@v4
        with:
          name: service-${{ matrix.os }}-${{ matrix.arch }}
          path: apps/server/dist/pulse-server-${{ matrix.os }}-${{ matrix.arch }}
          if-no-files-found: error

  github-release:
    runs-on: ubuntu-latest
    needs: [validate, dashboard, binaries]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.validate.outputs.tag }}
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: service-*
          merge-multiple: true
      - uses: actions/download-artifact@v4
        with:
          name: dashboard-assets
          path: dist/dashboard
      - name: Package release
        run: |
          tar -czf dist/pulse-dashboard-assets.tar.gz -C dist/dashboard .
          cd dist
          sha256sum pulse-server-* pulse-dashboard-assets.tar.gz > checksums.txt
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.validate.outputs.tag }}
        run: |
          previous="$(git tag --list 'service-v*' --sort=-v:refname | grep -Fxv "${TAG}" | head -n1 || true)"
          args=(release create "${TAG}" dist/pulse-server-* dist/pulse-dashboard-assets.tar.gz dist/checksums.txt --verify-tag --latest=false --generate-notes)
          if [[ -n "${previous}" ]]; then
            args+=(--notes-start-tag "${previous}")
          fi
          gh "${args[@]}"

  image:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate.outputs.tag }}
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Resolve image owner
        id: image
        run: echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "${GITHUB_OUTPUT}"
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/server/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ steps.image.outputs.owner }}/pulse-server:${{ needs.validate.outputs.image_tag }}
            ghcr.io/${{ steps.image.outputs.owner }}/pulse-server:${{ needs.validate.outputs.tag }}
