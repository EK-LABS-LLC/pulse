---
title: Opencode
description: Capture Opencode tool calls, sessions, and run metadata in Pulse.
---

## Overview

Pulse installs an Opencode plugin that subscribes to Opencode events and forwards normalized event payloads to `pulse emit`.

All spans are tagged with `source: "opencode"`.

## Quick start

Requires a running Pulse trace service and a local Pulse CLI setup.

1. Configure Pulse.

```bash
pulse up --open
# or: pulse connect --api-url https://pulse.example.com --api-key pulse_sk_... --project-id my-project
```

First-time local setup installs integrations for detected agents. To install or
refresh the plugin later:

```bash
pulse install-hooks
pulse status
```

## Integration behavior

- Detection path: `~/.config/opencode/`
- Installed plugin path: `~/.config/opencode/plugin/pulse-plugin.ts`

`pulse connect` only modifies the Pulse plugin file and leaves other Opencode configuration untouched.

Safe to re-run:

- `pulse connect` is idempotent
- `pulse disconnect` removes only Pulse artifacts

## Commands

| Command | Description |
| --- | --- |
| `pulse up` | Start/bootstrap the managed local Pulse server |
| `pulse dashboard` | Open the local dashboard with one-time local login |
| `pulse connect` | Install or refresh the Opencode plugin, or configure a remote Pulse target when flags are provided |
| `pulse install-hooks` | Install or refresh the Opencode plugin without changing config |
| `pulse disconnect` | Remove the Pulse-managed Opencode plugin |
| `pulse status` | Show config, connectivity, and plugin status |

## Events captured

| Opencode event | Pulse event | Notes |
| --- | --- | --- |
| `session.created` | `session_start` | Session starts |
| `session.idle` | `session_end` | `reason="idle"` |
| `session.error` | `session_end` | `reason="error"` plus error details |
| `message.updated` (user) | `user_prompt_submit` | Captures prompt text |
| `message.updated` (assistant) | `assistant_message` | Captures output, tokens, cost when available |
| `tool.execute.before` | `pre_tool_use` | Captures tool name, input, ID |
| `tool.execute.after` | `post_tool_use` | Captures tool response |

## Trace and span fields

Opencode events are emitted as OTLP spans. User prompt events start an
`agent.turn` trace; assistant and tool events attach to that trace when
possible. Session start/end lifecycle spans remain queryable but do not appear
as traces or increase session trace totals.

| Field | Description |
| --- | --- |
| `traceId` | OTel trace identifier |
| `spanId` | OTel span identifier |
| `pulse.session.id` | Opencode session identifier |
| `pulse.session.name` | Human-readable session name when available |
| `source` | `opencode` |
| `kind` | High-level event category |
| `event_type` | Specific event type |
| `status` | `success` or `error` |
| `tool_name` | Tool name for tool events |
| `tool_input` | Serialized tool input |
| `tool_response` | Tool output payload |
| `error` | Error payload for failed events |
| `metadata` | Project ID, CLI version, and extra event metadata |

## Auto-update behavior

After updating Pulse, refresh the bundled plugin and verify it:

```bash
pulse update
pulse install-hooks
pulse status
```

## Debugging

Enable debug logs when validating a new integration setup:

```bash
export PULSE_DEBUG=1
```

Then run Opencode once and inspect:

```bash
pulse status
```

For CLI-wide troubleshooting, see [CLI Reference](/docs/cli/).
